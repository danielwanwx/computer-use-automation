# 给 Codex：实现一个可学习、可复用的银行后台 UI 自动化系统

版本 2.2。收到本文件后，直接按下文设计、实现和验证，不需要用户另写启动 prompt。本文件取代旧执行稿；原始 Assignment A 如已附上，提交要求以原题为准。

配套技术设计为 `Computer_Use_System_Design_Spec.md`。本文件定义场景、范围和验收；Spec 定义组件、方法、数据契约与算法。两份一起阅读后直接实施，旧 v1 不再作为实施规范。将 Spec 保存到项目 `docs/DESIGN_SPEC.md`，按当前阶段读取相关章节，不必把全文反复注入每个 Agent。

## 1. 产品场景：谁在用，为什么需要它

银行客服正在帮助会员查询储蓄账户的可用余额。上游 AI 助手已经知道会员和账户，但银行的老旧后台没有可用 API，只能由工作人员打开页面、搜索会员、选择账户、读取余额。

我们要实现这个操作层：工程人员第一次给它一个自然语言任务，它通过 LLM 观察并操作真实页面，完成任务后保存一份可复用的“能力文件”。以后上游助手只需选择对应客户会话并传入新账户参数，系统按文件执行，不再让模型重新规划。遇到无法安全处理的页面，系统暂停，请工作人员接管当前浏览器，处理后再继续。

本次用本地 ParaBank 原生客户网银作为真实 UI 代理目标，完整实现“查询当前已登录客户的指定储蓄账户可用余额”。真实业务背景是银行后台，但演示不虚构 ParaBank 的员工搜索功能。交付一个本地可运行、可演示、可测试的小产品，覆盖学习、复用、异常处理和人工接管。暂不做开户、转账、生产部署、桌面自动化或多租户管理后台。

三类使用者及入口：

| 使用者 | 要完成的事 | 本次提供的入口 |
|---|---|---|
| 集成工程师 | 教系统完成第一次查询，检查并批准生成的能力 | 操作页面：输入任务、启动探索、查看和批准能力 |
| 上游 AI 助手 | 在指定客户会话中给出账户，获得结构化查询结果 | typed invoke 接口与 CLI；操作页面提供同一接口的演示入口 |
| 银行操作员 | 查看暂停原因，接管同一个浏览器，处理后归还 | 最小 operator 页面：状态、原因、安全证据、Claim / Resume / Abort |

## 2. 一条完整的使用故事

### 第一次：学习如何查询

工程师在操作页面选择 ParaBank 和已准备好的合成客户会话，输入：“查询我的储蓄账户 12345 的可用余额”。账户号只是示例，演示时使用实际 seed 结果。参数可以另外提供，但用户不必手写占位符或操作步骤。

系统从已登录的原生客户页面开始。LLM 根据当前页面操作：进入 Accounts Overview → 在完整加载的账户列表中找到目标账户 → 打开 Account Details → 确认账户编号及 SAVINGS 类型 → 读取 Available Balance。系统验证会话主体、账户归属证据和字段语义，不能仅凭模型说 done 就成功。

成功后生成能力 `get_savings_balance(account_id)`，包含输入输出、操作步骤、目标定位、成功条件及来源记录。调用上下文另外绑定 `session_id` 和会话管理器登记的期望客户身份，不允许调用者随意改写该身份。工程师可查看它、验证它并批准使用。录下的是如何查，不能录成固定答案。

### 第二次：换一个会员直接复用

上游调用：

```json
{"capability":"get_savings_balance","session_id":"demo-customer-b","inputs":{"account_id":"12456"}}
```

系统加载已批准的能力，在 UI 上执行相同流程，返回当前可用余额：

```json
{"status":"SUCCESS","outputs":{"available_balance":"2450.75","currency":"USD"}}
```

这里的金额仅为合成示例，实际输出必须来自本次页面。即使模型凭据已移除，查询仍能完成。后台余额改变后再次查询，应读到新值。

### 第三次：查询中断，需要人工接管

查询时出现系统不认识的验证提示。自动化停止操作，operator 页面显示暂停原因、当前步骤和安全页面证据。工作人员点击 Claim，在原来的浏览器中关闭或完成验证，再点击 Resume。

系统重新检查当前会员、账户及步骤状态。若人工已完成该步骤，就继续下一步；若仍在安全起点，就继续原步骤；若人误切换到其他会员，则保持暂停。不能新开浏览器冒充接管，也不能按下 Resume 就盲目执行。

## 3. 演示应用与业务规则

主目标固定为本地部署的 [ParaBank](https://github.com/parasoft/parabank) 原生页面，固定可运行版本并提供启动、seed、reset。不要重写它的业务页面，也不要再造 Staff Console。若无法启动，先诊断并继续不依赖它的工作，明确报告阻塞，不静默替换成自建应用。

准备至少 3 个合成客户、4 个账户，其中一个客户有两个储蓄账户。首次探索只使用一个客户，其余用于验证复用。认证作为准备步骤，通过 UI 建立客户会话，凭据从环境读取；能力只接收 account_id，不接收密码，也不在 replay 中自动换客户。 服务端将 session_id 绑定到受控 UI 登录验证过的预期客户身份和浏览器会话，调用者不能改变绑定。未知 session_id 在操作 UI 前返回 INPUT_INVALID，浏览器丢失返回 FAILURE / SESSION_LOST，会话过期进入 WAITING_FOR_HUMAN；人工重新登录必须与原身份重新核验。

身份验证使用受控登录会话、页面可见登录信息和该会话 Accounts Overview 的账户成员关系共同判断，再检查详情账户号与类型。不要从隐藏 JavaScript 变量读取客户 ID，也不要把可见姓名当作唯一强身份凭据。用户请求上下文、会话和页面证据不一致就停止；人工重新登录后须重新验证。币种通过固定 USD 的目标配置及页面格式验证，明确这是代理环境约定。

采用原生账户表格做关系定位验证，不强迫 ParaBank 增加 iframe。额外小型页面仅用于 frame/stale-ref 单测，不替代真实目标。异常由隔离的测试 harness 注入，如延迟、请求失败或阻塞弹窗；每份证据标明原生或注入场景，runtime 只能观察页面，不能读取场景开关。

自动化只能经 UI 完成任务，不能直接请求 ParaBank API、读数据库或网络响应取得答案；原生网页自身发出的正常请求可以运行。测试初始化和独立 evaluator 可从后端读取真值，但必须与 runtime 隔离。Available Balance 的 oracle 应核实所选版本页面定义，不能不加区分地拿账面 balance 对比；测试包含负余额边界。不要给 discovery 预写操作步骤。

查询结果使用下列语义：

| 情况 | 系统应如何响应 |
|---|---|
| 找到指定会员的指定储蓄账户 | SUCCESS，返回 available_balance 和 currency |
| 当前客户完整加载的账户列表没有请求的账户 | BUSINESS_OUTCOME / ACCOUNT_NOT_FOUND；不推断该账户在全行不存在 |
| 应用明确显示无访问权限 | BUSINESS_OUTCOME / ACCESS_DENIED，不继续尝试访问 |
| account_id 缺失或输入格式不合法 | INPUT_INVALID，在操作 UI 前拒绝；不得默认选第一个账户 |
| 页面加载暂时失败 | 在确认重试安全后最多额外尝试 2 次；仍失败则 FAILURE / RECOVERY_EXHAUSTED |
| 页面显示另一个会员或账户 | FAILURE / SUBJECT_MISMATCH，不返回余额 |
| 目标定位到多个元素 | FAILURE / TARGET_AMBIGUOUS，不选第一个 |
| 未知弹窗、会话过期或无法确认当前状态 | WAITING_FOR_HUMAN，发出介入请求；这是非终态 |
| 人工取消 | ABORTED |

SUCCESS 以外不返回余额。失败结果包含 run_id、错误码、步骤、安全诊断和证据引用；business outcome 是合法业务回答，不能当程序崩溃。

## 4. 必须实现的能力

### A. 真实的 LLM discovery

接受 goal 和 target，执行 observe → decide → act，单次最多 20 个模型决策、180 秒。模型只选择当前观察中的控件和允许动作；控件引用须绑定页面、frame 和观察版本，导航后旧引用失效。动作前执行策略检查，完成后验证业务状态。

至少保存一次真实模型驱动的成功记录。mock model 只能用于测试，不能作为 discovery 交付证据。

### B. 可审查的能力文件

使用 typed、versioned YAML 或 JSON，至少表达：名称、输入、输出、步骤、定位策略、条件、业务结果、恢复规则、最终成功条件。

每个探索得到的步骤链接来源事件；预先声明的策略和后来补充的异常规则分别标记，不能说一次成功探索学会了所有错误分支。参数通过绑定替换，不保存具体客户值或余额。

批准记录绑定完整可执行内容，包括引用的条件、parser 和定位配置；改动这些内容必须重新验证和批准。未定义引用或不受支持的动作在执行前拒绝。合成只读 demo 可由独立 reviewer Agent 审查批准，记录 agent 身份，不伪称人审。

### C. 确定性 replay

replay 根据能力文件执行，定位、提取、恢复和最终验证都不得调用模型。固定定位优先级，要求唯一匹配；每个关键步骤验证效果，最终按第 3 节验证目标应用、会话主体、账户成员关系、账户编号、储蓄类型、币种和字段语义。

金额使用 Decimal 并以字符串输出。timeout 不代表动作未发生：先观察是否已经完成，再决定重试；不知道效果时停下或请求人工处理。不能在恢复时无条件重复已完成动作。

### D. 同会话人工接管

自动化状态为 Running → Pausing → Waiting for human → Human controlled → Resuming。先停止新动作并处理在途操作，再交控制权；同一 session 串行操作，用 ownership/epoch 拒绝旧请求。记录接管时间、人工动作类别和恢复结果，不记录敏感输入值。

本次允许本机 headed 浏览器加最小控制页面。它是合作式接管，不承诺阻止真人提前用鼠标点击。浏览器进程丢失就报告 SESSION_LOST，不能伪造续接。

### E. 安全和可调试证据

所有动作共用 policy gateway，校验目标 origin/route、动作类型和业务风险。只允许查询所需操作；即使模型或能力文件把转账按钮声明为只读，仍须阻止。解析通过不等于获得权限，能力不能扩大部署策略。

给调用者的结果和保存的审计记录分开。日志只存安全字段、状态与引用，凭据、token、原始客户数据和余额不落盘；失败时至少保存一份可定位问题的脱敏截图或白名单页面结构快照。默认关闭原始 trace、HAR 和视频；截图不能安全遮罩时使用安全结构快照。

operator 控制接口绑定本机，验证 session-bound token 和请求来源，拒绝越权接管/恢复。明确本地受控环境的限制，不把浏览器 allowlist 宣传为完整网络沙箱。

### F. 薄的操作页面

页面只提供三个区域：

- **任务运行**：选择目标及已有客户会话、输入自然语言任务、启动 discovery；显示步骤进度与成功/业务结果/失败/等待人工状态。一个会话同时只允许一个运行。
- **能力与复用**：查看生成能力的输入、步骤、来源、验证结果及 draft/approved 状态；Approve 后填写新的 account_id 并选择匹配会话运行 replay。未批准时禁止普通 replay。提供受策略约束的 Validate 操作，可在隔离测试会话中验证 draft；验证通过后才允许批准，不得借此绕过普通 replay 的批准要求。
- **人工介入**：显示暂停原因、安全证据和 Claim/Resume/Abort，指明需要操作哪个本机浏览器窗口。只在人已获控制权后允许 Resume，不把页面按钮模拟成真实接管。

任务页面经本机访问校验后可临时显示本次授权结果，不把余额写入日志、URL、localStorage 或缓存。页面和 CLI 的输入校验、状态与返回值一致，错误需要说明原因。做浏览器端到端测试覆盖这三个区域；无需聊天界面、用户管理、复杂看板或远程屏幕流。

## 5. 实施顺序与 Agent 分工

使用 Python、Playwright、Pydantic 和 FastAPI，模块化单体。FastAPI 提供薄的操作页面，CLI 共用同一个应用服务层，不另写一套执行逻辑。内部划分 discovery、compiler、replay、surface、policy、handoff、evidence；浏览器感知和操作通过 surface 接口，便于以后替换成桌面 adapter。多租户复用只需设计说明：共享能力加受控配置、兼容性检查、变更后重新验证。

按以下顺序实际执行，每步通过后直接进入下一步，不逐阶段请求用户确认：

| 阶段 | 做什么 | 进入下一步的条件 |
|---|---|---|
| 1. 场景与设计 | 阅读配套 Spec，核实目标版本和接口；写短 DESIGN.md 作为章节/模块索引及必要差异说明，独立 reviewer 检查映射，不重新设计另一套系统 | 业务规则没有冲突，每项需求能对应验收 |
| 2. 第一条闭环 | 启动 ParaBank、准备合成数据和操作页，打通最小安全执行层、真实 discovery、编译和 replay | 一次 live discovery 生成的能力能对新输入 replay |
| 3. 完整行为 | 加入业务结果、安全恢复、人工接管、敏感数据处理 | 第 3、4 节行为实现并有针对性测试 |
| 4. 独立验证与回归 | 执行第 6 节，修复后重跑受影响测试，集成后跑完整自动回归 | 自动验收通过，真人验证如实记录 |
| 5. 合成交付 | 干净环境复现，补齐 README、REPORT、evidence 和演示命令 | 第 7 节交付齐全、命令实测可用 |

你担任集成负责人，按需使用实现 Agent 和独立验证 Agent，最多 3 个上下文同时工作。任务只传当前需求、接口和相关文件；不同 Agent 不同时写同一文件，共享接口由负责人统一修改。验证 Agent 从业务规则写 expected result，不照抄实现结果。

关键逻辑用 TDD：先写能暴露错误行为的测试，再实现，再重构。每个任务完成后只更新一个 PROGRESS.md，记录已完成项、代码版本、测试证据、阻塞和下一步。新上下文读本文件、DESIGN.md、PROGRESS.md 以及 Spec 的当前任务相关章节即可继续，不重读全部聊天。

同一根因两轮修复仍失败时，由负责人判断并缩小方案或报告具体阻塞，不无限重试。缺模型 key 时继续完成离线模块和测试，保留 live 验证待办。没有多 Agent 工具时可串行执行，但不要称为独立 Agent 审查。

## 6. 验收、指标与回归测试

测试判据必须独立于被测代码。独立验收器从测试侧读取 ParaBank 真值，runtime 只读 UI；给验收器一个格式正确但余额错误的结果，必须判失败。所有数据为合成数据。

| ID | 测试场景 | 通过标准 |
|---|---|---|
| V1 | 真实探索和能力来源 | 至少 1 次 live discovery 成功；生成步骤可追溯；模型在首页直接 done 会被拒绝 |
| V2 | 换输入重复查询 | discovery 未见过的 2 个会员各运行 5 次，10/10 输出匹配独立真值；过程中修改一次余额并验证读到新值 |
| V3 | 零模型依赖 | 移除模型凭据且设置调用即失败的模型入口后，正常查询和恢复仍通过；所有 replay 模型调用数为 0 |
| V4 | 业务与输入边界 | 账户不在当前客户列表、明确拒绝访问、缺 account_id 各返回第 3 节结果，均无余额泄漏 |
| V5 | 错误成功防护 | 错会员、错账户、错账户类型、重复定位均被拒绝；错误但格式正确的输出也无法通过 evaluator |
| V6 | 恢复与停止 | transient once 恢复成功；持续故障到重试上限停止；执行后回执丢失先检查效果，不盲目重复 |
| V7 | 控件与能力有效性 | stale observation、错误 frame、未定义条件被拒；批准后修改可执行依赖使旧批准失效 |
| V8 | 接管协议 | 在途操作交接、重复 claim、旧 epoch resume、越权操作有确定结果；人工切错会员不能继续 |
| V9 | 真实人接管 | 至少 1 次人在同一 session 操作后恢复成功；自动协议测试不代替这项 |
| V10 | 策略与数据保护 | 禁止目标和危险动作执行次数为 0；注入 canary 后，运行日志、DB 和证据中明文 canary 数为 0；失败 richer evidence 可用 |
| V11 | 干净环境复现 | 新 checkout 按 README 完成 setup 和无模型 replay；依赖固定，未借用开发目录的 DB 或浏览器状态 |
| V12 | 操作页面 | 从输入自然语言到能力批准和 replay 可完成；未批准/重复运行被阻止；等待人时显示原因并能接管、恢复或取消；页面与 CLI 结果一致 |

每个用例记录 PASS/FAIL/NOT_RUN、实际命令、源码版本、能力 digest、fixture seed 和证据路径。未运行不能计为通过。V2 的 10/10 仅代表本次受控测试，不是生产 SLA。

同时报告 discovery/replay 耗时、模型调用、可取得的 token 用量、重试次数和介入原因。耗时和 token 用于观察，不设无依据的速度或费用承诺。验收硬指标是正确结果、零模型 replay、无错误成功、有限恢复和真实接管。

回归规则：修改代码先跑相关单测和场景；集成及交付前跑完整自动矩阵。定位、编译或 schema 变化后重新生成并验证能力；discovery 路径变化后更新 live 证据。纯文档修改无需重复模型调用。最终证据必须对应交付代码，不能沿用失效的旧结果。

## 7. 最终交付和完成标准

交付以下文件，README/REPORT 使用英文，方便直接提交：

- 源码、固定依赖、配置模板、ParaBank 启动/合成数据脚本及一键 reset。
- README.md：setup、配置模型 key、discovery、inspect/approve、replay、真人 handoff 和测试的精确可复制命令；单列不需要模型 key 的 replay 路径。
- docs/DESIGN_SPEC.md：配套完整技术设计；DESIGN.md：实际模块索引与实现差异摘要。REPORT.md 控制在约 1–3 页，使用原题七个标题：Architecture；Artifact schema；Determinism & error handling；Heterogeneity & multi-tenant；Escalation & handoff；Safety；Cuts。
- artifacts/：真实 discovery 生成并验证的能力。evidence/：已验证能力的示例副本、脱敏 discovery、成功 replay、异常 replay、接管证据、验收结果和索引。
- 一个 verification 入口，运行自动验收并列出 live/manual 未运行项；必要的第三方许可说明。

README 首先给出操作页面的启动命令和访问地址，然后列 CLI 与测试入口。最终演示应能依次展示：学习一次 → 换会员复用 → 更新余额再读 → not-found → 暂态恢复 → 错会员拒绝 → 人工接管恢复。核心全部通过才标记“可提交”；否则明确列出缺什么、已完成什么，以及最小补充操作。

完成后给用户项目绝对路径、最短运行命令、实际测试结果、证据位置和已知限制。题目最终要求 public GitHub repo，并将链接发给 assignments@interface.ai；准备好提交材料，但公开发布和发邮件须有用户授权，不自动发送。

## 8. 开始工作

在用户所指根目录下已有的 Projects 文件夹内创建新的干净目录 `computer-use-automation`。同名非空则加日期或编号，不能覆盖现有项目；找不到 Projects 才询问路径。将本文件和项目全部工作放入新目录，记录绝对路径；明确续接本任务时恢复原目录。

现在从第 5 节第一阶段开始，持续推进至交付，不只返回计划。不使用 Codex Goal 功能；本产品的自然语言 goal 输入仍须保留。普通设计和修复自行决定，只有缺凭据/权限、真人接管验证或最终外部发布需要用户时，集中提出最小请求，其余可做工作先完成。
